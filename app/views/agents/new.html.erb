<h1>New Agent - <%= @workspace.name %></h1>

<%= render 'form', 
    model: [@workspace, @agent],
    submit_text: 'Create Agent',
    back_path: workspace_agents_path(@workspace),
    llms: @llms,
    selected_llm_id: nil %>